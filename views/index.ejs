<%- include('partials/head') %>
<%- include('partials/navigation') %>
<main>
    <div class="container p-4">
        <div class="row">
            <div class="card-columns">
                <% images.forEach(function(image){ %>
                <div class="card shadow animate__animated animate__fadeInDown">
                    <img src="<%= image.path %>" alt="<%= image.title %>" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title"><%= image.title%></h5>
                        <p class="card-text"><%= image.description%></p>
                        <p class="card-text text-warning"><%= format(image.created_at)%></p>
                        <a href="/image/<%= image._id%>" class="btn btn-view w-100">View profile</a>
                    </div>
                </div>
                <% })%>
            </div>
        </div>
    </div>
</main>
<%- include('partials/footer') %>

